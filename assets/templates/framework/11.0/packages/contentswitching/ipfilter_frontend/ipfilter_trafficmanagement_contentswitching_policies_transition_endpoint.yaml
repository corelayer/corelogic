      - name: INIT_$FILTERTYPE_$IPVERSION_IPFILTER_NEXT_TENANT_$PROTOCOL
        tags:
          - endpoint_ipfilter
          - tenant_ipfilter
        fields:
          - id: name
            data: <<prefix>>_INIT_$FILTERTYPE_$IPVERSION_IPFILTER_NEXT_TENANT_$PROTOCOL
          - id: expression
            data: <<core.stringmaps.appexpert.stringmaps.CONFIG_TENANT/$ipversion_$protocol_value_ipfilter_isdisabled>>.NOT
        expressions:
          install: add cs policy <<name>> -rule q{<<expression>>}
          uninstall: rm cs policy <<name>>
      - name: INIT_$FILTERTYPE_$IPVERSION_IPFILTER_NEXT_CSVGROUP_$PROTOCOL
        tags:
          - endpoint_ipfilter
          - csvgroup_ipfilter
        fields:
          - id: name
            data: <<prefix>>_INIT_$FILTERTYPE_$IPVERSION_IPFILTER_NEXT_CSVGROUP_$PROTOCOL
          - id: expression
            data: <<core.stringmaps.appexpert.stringmaps.CONFIG_CSVGROUP/$ipversion_$protocol_value_ipfilter_isdisabled>>.NOT
        expressions:
          install: add cs policy <<name>> -rule q{<<expression>>}
          uninstall: rm cs policy <<name>>
      - name: INIT_$FILTERTYPE_$IPVERSION_IPFILTER_NEXT_CSV_$PROTOCOL
        tags:
          - endpoint_ipfilter
          - csv_ipfilter
        fields:
          - id: name
            data: <<prefix>>_INIT_$FILTERTYPE_$IPVERSION_IPFILTER_NEXT_CSV_$PROTOCOL
          - id: expression
            data: <<core.stringmaps.appexpert.stringmaps.CONFIG_CSV/$ipversion_$protocol_value_ipfilter_isdisabled>>.NOT
        expressions:
          install: add cs policy <<name>> -rule q{<<expression>>}
          uninstall: rm cs policy <<name>>
