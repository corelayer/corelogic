name: dns
sections:
  - name: trafficmanagement.dns.addressrecords
    elements:
      - name: DUMMY
        fields:
          - id: fqdn
            data: dummy.corelogic.local
          - id: address
            data: 169.254.0.1
        expressions:
          install: add dns addRec {{fqdn}} {{address}} -TTL 300
          uninstall: rm server {{name}}