name: dns
dependencies:
placeholders:
  - name:
    expression:
sections:
  - name: trafficmanagement.dns.addressrecords
    elements:
      - name: DUMMY
        fields:
          - id: fqdn
            value: dummy.corelogic.local
          - id: address
            value: 169.254.0.1
        expressions:
          install: add dns addRec {{fqdn}} {{address}} -TTL 300
          uninstall: rm server {{name}}