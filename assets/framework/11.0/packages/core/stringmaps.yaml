name: stringmaps
sections:
  - name: appexpert.stringmaps
    elements:
      - name: ENDPOINT
        fields:
          - id: name
            data: ${prefix}$ENDPOINT
        expressions:
          install: add policy stringmap {{name}}
          uninstall: rm policy stringmap {{name}}
      - name: TENANT
        fields:
          - id: name
            data: "{{prefix}}TENANT"
        expressions:
          install: add policy stringmap {{name}}
          uninstall: rm policy stringmap {{name}}
      - name: FQDN
        fields:
          - id: name
            data: "{{prefix}}FQDN"
        expressions:
          install: add policy stringmap {{name}}
          uninstall: rm policy stringmap {{name}}
      - name: CS
        fields:
          - id: name
            data: "{{prefix}}CS"
        expressions:
          install: add policy stringmap {{name}}
          uninstall: rm policy stringmap {{name}}
      - name: CSGROUP
        fields:
          - id: name
            data: "{{prefix}}CSGROUP"
        expressions:
          install: add policy stringmap {{name}}
          uninstall: rm policy stringmap {{name}}
      - name: IPFILTER_CS
        fields:
          - id: name
            data: "{{prefix}}IPFILTER_CS"
        expressions:
          install: add policy stringmap {{name}}
          uninstall: rm policy stringmap {{name}}
      - name: IPFILTER_CSGROUP
        fields:
          - id: name
            data: "{{prefix}}IPFILTER_CSGROUP"
        expressions:
          install: add policy stringmap {{name}}
          uninstall: rm policy stringmap {{name}}
      - name: IPFILTER_TENANT
        fields:
          - id: name
            data: "{{prefix}}IPFILTER_TENANT"
        expressions:
          install: add policy stringmap {{name}}
          uninstall: rm policy stringmap {{name}}