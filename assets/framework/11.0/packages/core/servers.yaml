name: servers
dependencies:
placeholders:
  - name:
    expression:
sections:
  - name: trafficmanagement.loadbalancing.servers
    elements:
      - name: DUMMY
        dependencies:
          - name: dnsdummy
            reference: core.servers.trafficmanagement.dns.addressrecords.DUMMY
        expressions:
          install: add server {{name}} %%dnsdummy%%
          uninstall: rm server {{name}}