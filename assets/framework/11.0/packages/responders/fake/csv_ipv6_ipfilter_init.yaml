name: init
tags:
  - fake
  - core
sections:
  - name: appexpert.responder.policylabels
    elements:
      - name: INIT_CSV_IPV6_IPFILTER_FAKE
        fields:
          - id: name
            data: <<prefix>>_INIT_CSV_IPV6_IPFILTER_FAKE
          
        expressions:
          install: add responder policylabel <<name>> -policylabeltype BASEPROTOCOL
          uninstall: rm responder policylabel <<name>>