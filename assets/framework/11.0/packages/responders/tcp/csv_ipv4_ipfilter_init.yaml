name: init
tags:
  - tcp
  - core
sections:
  - name: appexpert.responder.policylabels
    elements:
      - name: INIT_CSV_IPV4_IPFILTER_TCP
        fields:
          - id: name
            data: <<prefix>>_INIT_CSV_IPV4_IPFILTER_TCP
          
        expressions:
          install: add responder policylabel <<name>> -policylabeltype OTHERTCP
          uninstall: rm responder policylabel <<name>>