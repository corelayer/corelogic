      - name: $FILTERTYPE_$IPVERSION_IPFILTER_CONFIGISSUE_$PROTOCOL
        fields:
          - id: name
            data: <<prefix>>_$FILTERTYPE_$IPVERSION_IPFILTER_CONFIGISSUE_$PROTOCOL
          - id: loglevel
            data: DEBUG
          - id: expression
            data: ("ipfilter_system_auditing_messageactions - CONFIG ISSUE - $FILTERTYPE - $IPVERSION - $PROTOCOL")
        expressions:
          install:  add audit messageaction <<name>> <<loglevel>> q{<<expression>>}
          uninstall: rm audit messageaction <<name>>
